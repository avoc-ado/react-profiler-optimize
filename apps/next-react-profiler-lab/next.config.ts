import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* keep defaults; compiler optimizations can hide memoization footguns in this profiling lab */
};

export default nextConfig;
